import React from 'react'

const SubmittedQuotes = () => {

    // console.log(quote)

    // const {comment, completed, modified_date, plants, user_id} = quote

    return (
        <div>
            {/* <p>{comment}</p>
            <p>{completed}</p>
            <p>{modified_date}</p>
            <p>{plants}</p>
            <p>{user_id}</p> */}

            <p>SubmittedQuotes</p>
        </div>
    )
}

export default SubmittedQuotes
